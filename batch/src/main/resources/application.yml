spring:
  application:
    name: snutt-batch
  config:
    import: classpath:application-core.yml
  batch:
    # 잡 선택은 JobRunner가 JOB_NAME으로 수행한다 (k8s CronJob, PLAN.md §4)
    job:
      enabled: false
    jdbc:
      initialize-schema: always
